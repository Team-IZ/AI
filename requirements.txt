# Big Project AI service — Phase 1 최소 의존성
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic-settings>=2.4,<3.0
python-multipart>=0.0.9        # Phase 2 ZIP 업로드 대비
httpx>=0.27,<1.0               # NVIDIA API 호출(Phase 3+) 및 테스트 클라이언트

# dev/test
pytest>=8.0,<9.0
