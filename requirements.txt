# Big Project AI service — 런타임 의존성
#
# **버전을 고정한다.** 범위(`>=`)로 두면 빌드 시점마다 다른 버전이 깔려
# "로컬은 되는데 배포는 안 됨"이 재현 불가능한 형태로 나타난다. 배포는 이 파일을
# 그대로 `pip install -r`로 깐다(apprunner.yaml).
#
# 올릴 때: 로컬에서 올리고 pytest 통과를 확인한 뒤 이 파일을 갱신한다.
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32        # ZIP·PDF 업로드(multipart)
httpx==0.28.1                   # NVIDIA API 호출 및 테스트 클라이언트

# 교안 PDF 텍스트 추출. 순수 파이썬이라 poppler 같은 시스템 바이너리를 안 쓴다 —
# 관리형 런타임(App Runner)에서는 시스템 패키지를 깔 수 없다.
pypdf==6.14.2
