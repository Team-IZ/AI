# D1 (2026-07-31, app/engines/codemap/crew.py 모듈 docstring): 더 이상 어떤 코드도
# 이 파일을 필요로 하지 않는다 -- Tier 2 재랭킹(crew.py)이 crewai 대신
# app.engines.shared.llm.chat() + app.engines.shared.agent_loop(도구 사용이 실제로
# 필요해지면)로 옮겨갔다. 이 파일은 참고용으로만 남긴다: crewAIInc/nvidia-demo
# 기반으로 실제 동작을 확인했던 버전 조합 기록(재도입할 일이 생기면 여기서 시작).
# pip install -r requirements-codemap.txt 를 실행하는 CI job/문서 절차는 없다.
crewai==0.95.0
crewai-tools==0.25.8
litellm==1.57.4
openai==1.59.8
