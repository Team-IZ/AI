# 개발·테스트 전용. **배포 이미지에는 안 들어간다** — Dockerfile은 requirements.txt만 설치한다.
-r requirements.txt

pytest==8.4.2
pytest-cov==7.1.0
moto[dynamodb]==5.2.3   # tests/test_job_store.py -- 실제 AWS 없이 DynamoDbJobStore 검증
